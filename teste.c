#include <stdio.h>

int main()
{
int i, num, x;

  printf("Digite um numero:");
  scanf("%d",&num);
  
  for(i=1;i<=9;i++)
  {
      num * i = x;
      printf("%d + %d = %d\n",num,i,x);
  }
  return 0;
  }

